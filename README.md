# rade_v2_voice

A pure-C, BSD-licensed implementation of the `rv_*` DLL interface used by
[RadeCallTest](https://github.com/yi-matsui/RadeCallTest) style Windows applications, built on
[RADE](https://github.com/drowe67/radae) (Radio Autoencoder) V1/V2 and the
[FARGAN](https://arxiv.org/abs/2405.21069) vocoder.

## Quick Start (Windows)

One command builds everything — the FARGAN model data download, `opus.lib`,
and `rade_v2_voice.dll`:

1. Install **Visual Studio 2022** with the **"Desktop development with C++"**
   workload (includes MSVC, CMake, and NMake).
2. Open the **"x64 Native Tools Command Prompt for VS 2022"** from the Start
   menu (search for "x64 Native").
3. Run:

```bat
git clone https://github.com/yi-matsui/rade_v2_voice.git
cd rade_v2_voice
build.bat
```

The first run downloads the FARGAN model data (network access required, once)
and builds `opus.lib` (a few minutes). The result is
`src_v2\rade_v2_voice.dll`. Re-running `build.bat` skips the steps that are
already done and only rebuilds the DLL.

## Background

RadeCallTest originally linked against a `rade_voice.dll` implementation that
is GPL-licensed. `rade_v2_voice.dll` in this repo re-implements the same
19-function `rv_*` interface (verified against the original via
`dumpbin /exports`) from scratch in pure C, so it can be distributed under a
permissive BSD license. It links RADE V2 (primary) and RADE V1 (`src_v1`,
compatibility/interop) against Opus/LPCNet's FARGAN vocoder.

## Layout

```text
rade_v2_voice/
├── build.bat                  ← one-shot build script (start here)
├── README.md / LICENSE / NOTICE
├── src_v2/                    RADE V2 codec + rv_* DLL entry points
│   ├── rade_v2_voice.c        rv_* interface implementation (DLL main source)
│   ├── rade_api_v2.c/.h       V2 public C API
│   ├── rade_enc_v2.c / rade_dec_v2.c / rade_tx_v2.c / rade_rx_v2.c
│   │                          encoder / decoder / TX / RX pipelines
│   ├── rade_sync_v2.c / rade_frame_sync.c / rade_extract_v2.c
│   │                          acquisition, frame sync, symbol extraction
│   ├── rade_eoo_v2.c / rade_bpf_v2.c
│   │                          End-of-Over (callsign) and band-pass filter
│   ├── *_data.c/.h            trained model weights baked in as C arrays
│   ├── kiss_fft.c/.h          KissFFT (BSD)
│   ├── ht_coeff.h             Hilbert transformer coefficients
│   ├── build/                 MSVC batch build scripts
│   │   ├── build_rade_v2_voice.bat   ← builds rade_v2_voice.dll
│   │   ├── build_rade_v2_dll.bat     builds rade_v2.dll (V2-only, no rv_*)
│   │   └── build_test_*.bat          per-stage test builds
│   ├── tests/                 standalone C test programs (loopback etc.)
│   ├── csharp/                C# P/Invoke wrappers (RadeV2Native.cs) + harness
│   └── doc/                   design notes (DLL ABI, BPF, V2 spec patch)
├── src_v1/                    RADE V1 pure-C port (V1 interop, rade_api_nopy.c)
├── third_party/
│   └── opus/                  vendored xiph/opus @ 940d4e5a (FARGAN/LPCNet)
│       └── build/opus.lib     ← generated by build.bat (git-ignored)
├── tools/                     Python weight-export scripts (.pth → *_data.c)
├── patches/                   opus-nnet.h.diff (Linux/macOS builds only)
├── scripts/                   build_opus.bat (standalone opus checkout use)
└── docs/                      background reference documents
```

| Path | Contents |
| --- | --- |
| `src_v2/` | RADE V2 codec (encoder/decoder/frame-sync/EOO/BPF) and `rade_v2_voice.c`, the `rv_*` DLL entry points |
| `src_v2/tests/` | Standalone C test programs for individual V2 stages (sync, enc, dec, EOO, BPF, loopback, ...) |
| `src_v2/build/` | Windows (`cl.exe`/MSVC) batch build scripts |
| `src_v2/csharp/` | C# `P/Invoke` wrappers and smoke-test harness for consuming the DLL from .NET |
| `src_v2/doc/` | Design notes on the DLL ABI, BPF/pitch limiting, and the V2 spec patch |
| `src_v1/` | RADE V1 pure-C port (encoder/decoder/OFDM/acquisition), used for V1 interop |
| `tools/` | Python scripts that export trained PyTorch weights into the `*_data.c` arrays compiled into `src_v1`/`src_v2` |
| `third_party/opus` | Vendored copy of [xiph/opus](https://github.com/xiph/opus) at commit `940d4e5a`, providing FARGAN/LPCNet (build outputs and downloadable model data excluded) |
| `patches/` | `opus-nnet.h.diff` — applied to `third_party/opus/dnn/nnet.h` on Linux/macOS builds only (not needed for the MSVC static-link build) |
| `scripts/` | `build_opus.bat` — builds `opus.lib` on Windows via CMake/NMake, including FARGAN model data download |

`rade_v2_constants_data.c`, `rade_enc_v2_data.c`, `rade_dec_v2_data.c` (and
their V1 equivalents in `src_v1/`) contain the trained model weights baked in
as C arrays, generated by the scripts in `tools/`. They're large text files
(tens of MB) but are tracked directly in this repo rather than via Git LFS.

## Getting the Opus/LPCNet dependency

FARGAN and the LPCNet feature extraction (`arch.h`, `lpcnet.h`, `freq.h`,
`os_support.h`, `fargan.h`, `cpu_support.h`) come from Xiph's Opus repo. The
full source tree is vendored in this repository under `third_party/opus`, at
commit `940d4e5af64351ca8ba8390df3f555484c567fbb` — the exact commit this
project was built and verified against (the same one
`scripts/build_opus.bat` and the original `BuildOpus.cmake` pin to). No
submodule setup is needed; a plain `git clone` gets everything.

The vendored tree intentionally **excludes the trained model data**
(FARGAN/DRED/OSCE weights: `dnn/*_data.c`, `dnn/*_data.h`,
`dnn/models/*.pth`, `dnn/dred_rdovae_constants.h`). Opus doesn't ship this
data in its own repo either — it must be downloaded once before building:

```bat
cd third_party\opus
dnn\download_model.bat 4ed9445b96698bad25d852e912b41495ddfa30c8dbc8a55f9cde5826ed793453
```

(On Linux/macOS: `./dnn/download_model.sh 4ed9445b...` — same hash.) These
downloaded files are listed in `.gitignore`, so they won't show up as
untracked changes.

### Building opus itself

On Linux/macOS, Opus builds the normal autotools way, with DNN/OSCE and
FARGAN enabled. This path also needs `patches/opus-nnet.h.diff` applied to
`dnn/nnet.h` first (adds a `RADE_EXPORT` visibility macro and generic-layer
function declarations — matches `BuildOpus.cmake`'s `PATCH_COMMAND`):

```bash
patch third_party/opus/dnn/nnet.h patches/opus-nnet.h.diff
cd third_party/opus
./autogen.sh
./configure --enable-osce --enable-dred --disable-shared --disable-doc --disable-extra-programs
make
```

On Windows, build from an **x64 Native Tools Command Prompt for VS**, with
`cmake`, `tar`, and `powershell` on `PATH`. Since the opus source is already
vendored in-tree, run the model download (above) once, then configure and
build directly in `third_party\opus`:

```bat
cd third_party\opus
cmake -B build -G "NMake Makefiles" ^
  -DOPUS_BUILD_SHARED_LIBRARY=OFF -DOPUS_BUILD_TESTING=OFF ^
  -DOPUS_DEEP_PLC=ON -DOPUS_DRED=ON -DOPUS_OSCE=ON
cmake --build build
```

This produces `third_party\opus\build\opus.lib` (git-ignored). Note that
`patches/opus-nnet.h.diff` is **not applied** on this path — it addresses
GCC visibility attributes and isn't needed when statically linking opus into
the DLL under MSVC.

`scripts/build_opus.bat` automates the same steps (same commit, same model
hash, same CMake options), but it was written for a standalone checkout: it
clones `opus` into its working directory if not present. If you prefer the
script, run it in a scratch location, or adjust its clone step to reuse
`third_party/opus`.

## Building rade_v2_voice.dll (Windows / MSVC)

The easiest path is `build.bat` at the repository root (see Quick Start) —
it runs the model download, the opus build, and the DLL build in order,
skipping whatever is already done.

To run just the DLL step manually, from `src_v2/` in an
"x64 Native Tools Command Prompt":

```bat
cd src_v2
build\build_rade_v2_voice.bat
```

The build scripts under `src_v2/build/` default to the vendored opus tree
(`OPUS_SRC=..\third_party\opus`, `OPUS_LIB=..\third_party\opus\build\opus.lib`,
`V1_SRC=..\src_v1`, all relative to `src_v2/`), so no environment variables
are needed for a plain clone of this repo. Set `OPUS_SRC` / `OPUS_LIB` only
if you want to build against an opus tree somewhere else.

This produces `rade_v2_voice.dll`, exporting the `rv_*` functions consumed by
`src_v2/csharp/RadeV2Native.cs`.

### Optimization flags (important)

`build_rade_v2_voice.bat` and `build_rade_v2_dll.bat` compile with
`/O2 /Oi /fp:fast /arch:AVX /DNDEBUG`. Do **not** build these DLLs with
`/Od`: an unoptimized build makes the RX pipeline take ~290 ms per 100 ms of
input audio, so real-time reception drops more than half of the samples and
decodes only intermittently. With `/O2` the same processing takes ~16 ms per
100 ms (about 18× faster) and sync holds continuously. `/arch:AVX` is used
rather than `/arch:AVX2` because AVX2-less CPUs crash with an illegal
instruction (0xC000001D); AVX covers essentially all Intel/AMD CPUs from
2011 onward.

## License

This project is licensed under the **BSD 2-Clause License**. See the `LICENSE` file for details.

### Third-Party Components and Licenses
This project reuses and incorporates several third-party components:

* **RADE:** Based on the [RADE](https://github.com/drowe67/radae) project, licensed under BSD 2-Clause (© David Rowe).
* **Opus:** `third_party/opus` is a vendored copy of Xiph's Opus project, licensed under **BSD 3-Clause** (© Xiph.Org Foundation and contributors) — see `third_party/opus/COPYING`.
* **Opus DNN/FARGAN:** Includes kernels and model weight export tools licensed under **BSD 2-Clause** (© Amazon / Jan Buethe).
* **KissFFT:** Used for signal processing in `src_v2/` and `src_v1/`, licensed under **BSD 3-Clause** (© Mark Borgerding).

**Note on Redistribution:**
Third-party copyright notices and license texts are bundled in the `NOTICE` file. When redistributing this codec in any form, you **must include the `NOTICE` file** to comply with the license requirements of the individual components.

## Acknowledgments

This project would not have been possible without the foundational work of the following projects and individuals:

* **David Rowe (drowe67):** For creating [RADE](https://github.com/drowe67/radae) and [FreeDV](https://freedv.org/), which provided the essential framework for HF digital voice communication.
* **Xiph.Org Foundation:** For their incredible work on [Opus](https://opus-codec.org/), LPCNet, and FARGAN, which made high-quality neural voice coding feasible for amateur radio applications.
* **The FreeDV Community:** For continuous feedback and testing, which helped refine the implementation and improve interop.
* **Mark Borgerding:** For his portable and efficient [KissFFT](https://github.com/mborgerding/kissfft) implementation.

Special thanks to the early testers who provided invaluable debugging insights during the development of this pure-C port.

## Reference

D. Rowe, J.-M. Valin, [RADE: A Neural Codec for Transmitting Speech over HF
Radio Channels](https://arxiv.org/abs/2505.06671), arXiv:2505.06671, 2025.

`docs/README_MSVC_upstream_rade_dll.md` documents building the upstream
`drowe67/radae` Python-embedded C API (`rade.dll`) with MSVC — a separate,
Python-dependent DLL, distinct from this repo's pure-C `rade_v2_voice.dll`.
Kept here as background reference for the opus/MSVC build reasoning.
